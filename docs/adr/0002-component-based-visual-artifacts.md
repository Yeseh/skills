# Component-based visual artifacts

Visual artifacts will be assembled from a small generic visual grammar and emitted as self-contained HTML using standard web components. Purpose-specific formats should use artifact adapters that compose generic components instead of adding report-specific component types. OpenAPI remains a specialized component because it is a frequently used, structured document format with substantial rendering behavior of its own.
